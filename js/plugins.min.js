(function () {
    var e;
    var d = function () {
    };
    var b = ["assert", "clear", "count", "debug", "dir", "dirxml", "error", "exception", "group", "groupCollapsed", "groupEnd", "info", "log", "markTimeline", "profile", "profileEnd", "table", "time", "timeEnd", "timeline", "timelineEnd", "timeStamp", "trace", "warn"];
    var c = b.length;
    var a = (window.console = window.console || {});
    while (c--) {
        e = b[c];
        if (!a[e]) {
            a[e] = d
        }
    }
}());
$(document).ready(function () {
    $(".navbar-nav a").click(function (d) {
        d.preventDefault();
        $(".navbar-nav > li").removeClass("active");
        $(this).parent("li").addClass("active")
    });
    $(".step-one").click(function () {
        $(this).addClass("move");
        var e = 0;
        var f = $(".work-step");
        d();
        function d() {
            if (e < f.length) {
                $(f[e]).addClass("active").delay(400).queue(function (g) {
                    $(this).addClass("move");
                    g()
                });
                e++;
                setTimeout(d, 1000)
            }
        }
    });
    var c;
    var b = 48.44765;
    var a = 35.020844;
    $(document).ready(function () {
        c = new GMaps({
            el: "#map",
            lat: b,
            lng: a,
            zoomControl: false,
            panControl: false,
            zoom: 17,
            streetViewControl: false,
            mapTypeControl: false,
            overviewMapControl: false,
            scrollwheel: false
        });
        c.addMarker({lat: b, lng: a})
    })
});